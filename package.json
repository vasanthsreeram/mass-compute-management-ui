{
  "name": "gpu-proxy",
  "private": true,
  "description": "Multi-tenant GPU rental proxy in front of Massed Compute",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "db:local": "wrangler d1 migrations apply gpu-proxy --local"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "wrangler": "^4.92.0"
  }
}
