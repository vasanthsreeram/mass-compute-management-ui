{
  "name": "massed-compute-management-ui",
  "private": false,
  "license": "MIT",
  "description": "Massed Compute Management UI — multi-tenant GPU rental proxy in front of one Massed Compute account",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/vasanthsreeram/mass-compute-management-ui.git"
  },
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "db:local": "wrangler d1 migrations apply gpu-proxy --local"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "wrangler": "^4.92.0"
  }
}
